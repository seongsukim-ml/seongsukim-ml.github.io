# 🚀 사이트 배포 가이드

이 문서는 완성된 블로그를 실제 웹사이트로 만드는 방법을 설명합니다.

## 📋 목차
1. [배포 옵션 비교](#배포-옵션-비교)
2. [GitHub Pages (추천)](#github-pages-추천)
3. [Netlify](#netlify)
4. [Vercel](#vercel)
5. [커스텀 도메인 설정](#커스텀-도메인-설정)

---

## 배포 옵션 비교

| 옵션 | 난이도 | 비용 | 속도 | 권장 대상 |
|------|--------|------|------|-----------|
| **GitHub Pages** | ⭐ 쉬움 | 무료 | 빠름 | 모든 사용자 (추천) |
| **Netlify** | ⭐⭐ 중간 | 무료 | 매우 빠름 | 고급 기능 필요 시 |
| **Vercel** | ⭐⭐ 중간 | 무료 | 매우 빠름 | 개발자 친화적 |

**추천**: GitHub Pages를 먼저 사용해보세요! 무료이고 설정이 간단하며, 나중에 다른 서비스로 쉽게 이전할 수 있습니다.

---

## GitHub Pages (추천)

### 장점
- ✅ **완전 무료**
- ✅ **설정 간단** (5분 소요)
- ✅ **자동 배포** (git push만 하면 됨)
- ✅ **HTTPS 기본 제공**
- ✅ **커스텀 도메인 지원**

### 단계별 설정

#### 1. GitHub 저장소 만들기

1. [GitHub](https://github.com)에 로그인
2. 우측 상단 `+` → `New repository` 클릭
3. 저장소 이름:
   - **개인 사이트**: `your-username.github.io` (예: `seongsukim-ml.github.io`)
   - **프로젝트 사이트**: 아무 이름 (예: `my-blog`)
4. **Public** 선택 (무료 플랜)
5. `Create repository` 클릭

#### 2. 코드 업로드

```bash
# 현재 디렉토리에서 (new_blog 폴더 안에서)
cd /path/to/new_blog

# GitHub 저장소 연결
git remote add origin https://github.com/your-username/your-repo.git

# 코드 푸시
git branch -M main
git push -u origin main
```

**참고**: `your-username`과 `your-repo`를 실제 값으로 변경하세요.

#### 3. GitHub Pages 활성화

1. GitHub 저장소 페이지에서 `Settings` 탭 클릭
2. 왼쪽 메뉴에서 `Pages` 클릭
3. **Source** 섹션:
   - Branch: `main` 선택
   - Folder: `/ (root)` 선택
4. `Save` 클릭

#### 4. 배포 완료!

몇 분 후 사이트가 다음 주소에서 접속 가능합니다:
- **개인 사이트**: `https://your-username.github.io`
- **프로젝트 사이트**: `https://your-username.github.io/your-repo`

---

## Netlify

### 장점
- ✅ **무료**
- ✅ **자동 배포**
- ✅ **CDN 제공** (전세계 빠른 속도)
- ✅ **폼 처리** (문의 폼 등)
- ✅ **리다이렉트 지원**

### 단계별 설정

#### 1. Netlify 계정 만들기

1. [Netlify](https://www.netlify.com)에 접속
2. `Sign up` 클릭
3. GitHub 계정으로 로그인

#### 2. 사이트 생성

1. `Add new site` → `Import an existing project` 클릭
2. `GitHub` 선택
3. 저장소 선택 (위에서 만든 것)
4. 설정:
   - **Branch to deploy**: `main`
   - **Build command**: (비워둠)
   - **Publish directory**: (비워둠 또는 `.`)
5. `Deploy site` 클릭

#### 3. 배포 완료!

사이트가 `https://random-name-123456.netlify.app` 주소에서 접속 가능합니다.

**사이트 이름 변경**:
- `Site settings` → `Site details` → `Change site name`

---

## Vercel

### 장점
- ✅ **무료**
- ✅ **초고속 CDN**
- ✅ **자동 배포**
- ✅ **개발자 친화적**

### 단계별 설정

#### 1. Vercel 계정 만들기

1. [Vercel](https://vercel.com)에 접속
2. `Sign up` 클릭
3. GitHub 계정으로 로그인

#### 2. 사이트 생성

1. `Add New` → `Project` 클릭
2. GitHub 저장소 선택
3. `Import` 클릭
4. 설정:
   - **Framework Preset**: `Other`
   - **Build Command**: (비워둠)
   - **Output Directory**: (비워둠 또는 `.`)
5. `Deploy` 클릭

#### 3. 배포 완료!

사이트가 `https://your-repo.vercel.app` 주소에서 접속 가능합니다.

---

## 커스텀 도메인 설정

자신만의 도메인(예: `seongsukim.com`)을 사용하고 싶다면:

### 1. 도메인 구매

**추천 서비스**:
- [Namecheap](https://www.namecheap.com) - 저렴함
- [Google Domains](https://domains.google) - 간편함
- [Cloudflare](https://www.cloudflare.com) - 무료 보안

**가격**: 연 $10-15 정도

### 2. DNS 설정

#### GitHub Pages:

도메인 제공업체에서 다음 레코드 추가:

```
Type: A
Name: @
Value: 185.199.108.153

Type: A
Name: @
Value: 185.199.109.153

Type: A
Name: @
Value: 185.199.110.153

Type: A
Name: @
Value: 185.199.111.153

Type: CNAME
Name: www
Value: your-username.github.io
```

그 다음:
1. GitHub 저장소 → `Settings` → `Pages`
2. `Custom domain`에 도메인 입력 (예: `seongsukim.com`)
3. `Enforce HTTPS` 체크

#### Netlify/Vercel:

1. 플랫폼에서 `Add custom domain` 클릭
2. 도메인 입력
3. 제공된 DNS 설정을 도메인 제공업체에 추가

---

## 🔄 업데이트 방법

사이트를 수정한 후 업데이트하는 방법:

### 1. 로컬에서 수정
```bash
# 파일 수정 (publications.json, news.json 등)
```

### 2. Git 커밋
```bash
git add .
git commit -m "Update publications"
git push
```

### 3. 자동 배포
- **GitHub Pages**: 2-3분 후 자동 배포
- **Netlify/Vercel**: 1분 후 자동 배포

---

## 📊 배포 전 체크리스트

배포하기 전에 확인하세요:

- [ ] `data/profile.json`의 정보가 정확한가요?
- [ ] `data/publications.json`이 최신인가요?
- [ ] `data/news.json`이 최신인가요?
- [ ] `data/authors.json`의 URL들이 정확한가요?
- [ ] 이메일 주소가 정확한가요?
- [ ] 모든 링크가 작동하나요?
- [ ] 프로필 사진이 있나요? (`assets/profile.jpg`)
- [ ] BibTeX 파일들이 올바른가요?

---

## 🐛 문제 해결

### Q1: 사이트가 배포되었는데 스타일이 깨져 보여요

**원인**: 상대 경로 문제

**해결**:
1. 프로젝트 사이트인 경우 (`username.github.io/repo-name`):
   - `index.html`의 모든 경로를 확인
   - `css/style.css` → `/repo-name/css/style.css`로 변경 필요

2. 개인 사이트인 경우 (`username.github.io`):
   - 이미 올바르게 설정됨

### Q2: 404 에러가 나요

**해결**:
- `index.html` 파일이 루트에 있는지 확인
- Branch가 `main`으로 설정되었는지 확인
- 대소문자가 정확한지 확인 (파일명)

### Q3: 변경사항이 반영되지 않아요

**해결**:
1. 브라우저 캐시 삭제 (Ctrl+F5 또는 Cmd+Shift+R)
2. GitHub Actions 확인 (저장소의 Actions 탭)
3. 배포 로그 확인

### Q4: HTTPS가 작동하지 않아요

**해결**:
- GitHub Pages: `Enforce HTTPS` 체크박스 활성화
- 커스텀 도메인: DNS 전파 대기 (최대 24시간)

---

## 🎯 추천 워크플로우

### 처음 시작할 때:
1. ✅ **GitHub Pages로 시작** (무료, 간단)
2. ✅ 사이트가 잘 작동하는지 확인
3. ✅ 필요시 커스텀 도메인 추가

### 나중에 업그레이드:
1. 더 빠른 속도가 필요하면 → **Netlify/Vercel**
2. 고급 기능이 필요하면 → **Netlify** (폼, 함수 등)
3. 간단하게 유지하려면 → **GitHub Pages** (계속 사용)

---

## 📚 참고 자료

### 공식 문서:
- [GitHub Pages 문서](https://docs.github.com/en/pages)
- [Netlify 문서](https://docs.netlify.com)
- [Vercel 문서](https://vercel.com/docs)

### 튜토리얼:
- [GitHub Pages 시작하기](https://pages.github.com)
- [커스텀 도메인 설정](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)

---

## 💡 팁

1. **GitHub Pages 추천 이유**:
   - 학술 사이트에 적합
   - 무료이고 신뢰성 높음
   - GitHub와 통합되어 관리 편함

2. **빠른 시작**:
   ```bash
   # 1. 저장소 만들기
   # 2. 코드 푸시
   git push origin main
   # 3. Settings → Pages에서 활성화
   # 끝!
   ```

3. **정기적 업데이트**:
   - 뉴스 추가: `data/news.json` 수정 → git push
   - 논문 추가: `data/publications.json` 수정 + `.bib` 파일 추가 → git push

---

궁금한 점이 있으면 언제든지 물어보세요! 🎉
